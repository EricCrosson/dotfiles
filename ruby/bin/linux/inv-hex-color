#!/usr/bin/env ruby
# Written by Eric Crosson
# 2017-01-07
#
# This program will invert a stream of hexadecimal color codes.  Omit
# the octothorpe (#) as it is commonly used as a comment character for
# terminal emulators.


def invert_hex_color(color)
  return color.tr("0123456789ABCDEF","FEDCBA9876543210")
end

def main
  ARGV.each do |arg|
    puts invert_hex_color arg
  end
end

if __FILE__ == $0
  main
end
