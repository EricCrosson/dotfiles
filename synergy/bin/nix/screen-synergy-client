#!/usr/bin/env bash
# Written by Eric Crosson
# 2017-01-10
#
# This program invokes synergy client (for use by a controlling screen
# session).
#
#
# Requirements:
#
# 1. expects env to contain ${SYNERGY_HOST} pointing to a synergy server

# TODO: kill USR1 to restart

readonly synergy_config="${HOME}/bin/nix/configure-synergy-client"
source ${synergy_config}

synergyc --no-daemon --name $(hostname) --restart ${SYNERGY_HOST}
