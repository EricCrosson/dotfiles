#!/usr/bin/env bash
# vim: filetype=sh

# apt-packages.txt hash: {{ include "apt-packages.txt" | sha256sum }}

{{ if (eq .chezmoi.os "linux") -}}

xargs -a ~/.local/share/chezmoi/apt-packages.txt sudo apt -y install

{{ end -}}
